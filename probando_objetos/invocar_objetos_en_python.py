# from archivo import Clase es lo que usás. Sin el .py
from clase_persona import Persona
# se crea un objeto persona con los datos que se necesitan para inicializarlos
p = Persona("Lourdes",32)
# se llama a función definida en la clase persona, es decir que se aplica a un objeto del tipo persona
p.saludar()